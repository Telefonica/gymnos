# Starter pipeline
# Start with a minimal pipeline that you can customize to build and deploy your code.
# Add steps that build, run tests, deploy, and more:
# https://aka.ms/yaml

trigger:
  branches:
    include:
    - devel
  paths:
    exclude:
    - docs/README.md
    - README.md

pool:
  vmImage: 'Ubuntu-16.04'

steps:
- script: |
    DATE=$(date +%Y-%m-%d-%H-%M-%S)
    echo Generate docker image from Dockerfile.devel
    docker build -t $(dockerId)/$(imageName)-$DATE -f Dockerfile.devel .
    echo Generate docker image from Dockerfile.devel
    docker build -t $(dockerId)/$(imageName)-gpu-$DATE -f Dockerfile.devel-gpu .
    echo Push images to DockerHub
    docker login -u $(dockerId) -p $(dockerPassword)
    docker push $(dockerId)/$(imageName)-$DATE
    docker push $(dockerId)/$(imageName)-gpu-$DATE
  displayName: 'Generate docker image and push it to DockerHub'
